-r requirements.minimal.txt

# Only required if using Postgres as a backend.
psycopg2==2.5.2

# Required for importing data from Google spreadsheets.
gspread==0.1.0

# Required for using Mandrill as an email backend.
djrill==1.2.0
